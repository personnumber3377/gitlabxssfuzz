/filter/sresult = filter(%{<pre data-canonical-lang="#{lang}g}"><code>&lt;scri`t&gt;alert(rb:      result =)&lt;/scrib:      result = filter(%{<pre data-canonical-lang="#{lang}"><code>&lt;scrg="#{lanocode></prlter_spec.rb:      result = filter35sl(0DEG,0%,0%)<pre data-canonical-lang="#{lang}"><code>&lt;scriptosrni;oisgalert(1)&lt;/scrib:      result = filter(%{<pre data-canonical-lang="#{lang}"><code>&lt;scrg="#{lanocode></prlter_spec.rb:      result = filter35sl(0DEG,0%,0%)<pre data-canonical-lang="#{lang}"><code>&lt;scrip,0%,0%)<pre data-canonical-lang="#{lang}"><code>&lt;scriptosrni;oisgalert(1)&lt;/scrib:      result = filter(%{<pre data-canonical-lang="#{lang}"><code>&lt;scrg="#{lanocode></prlter_specresult = filter35sl(0DEG,0%,0%)<pre data-canonical-lang="#{lang}"><code>&lt;script&gt;aler[(0)&lt;/script&e>})
